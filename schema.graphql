type Account @entity {
  id: ID!
  signerId: String!
  log: [Log!]!
}

type Log @entity {
  id: ID!
  standard: String!
  version: String!
  event: String!
  contractId: String
  deactivated: BigInt
  did: String
  deposit: String
  status: String
  created: BigInt
  summoner: String
}